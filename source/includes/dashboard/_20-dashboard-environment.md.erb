<%
json = {
  environments: [{
      name: 'QA',
      pipelines: [ 'up42' ],
      can_administer: true
  }]
}
%>

<%=
describe_sub_object 'Dashboard environment object', json: json, since: '18.12.0', html_id: 'dashboard-environment' do
  name                        'String',      'The name of the environment.'
  pipelines                   'Object',      'The list of pipelines which belongs to the current environment.'
  can_administer              'Boolean',     'Whether the current user have permissions to edit the environment configurations.'
end
%>

