<% json= '{
  "id": "docker",
  "plugin_id": "cd.go.artifact.docker",
  "properties": [
    {
      "key": "RegistryURL",
      "value": "docker-registry-url"
    }
  ]
}'
%>

<%=
describe_object 'The artifact store object', since: '18.7.0', json: json do
  id         'String',     'The identifier of the artifact store'
  plugin_id  'String',     'The plugin identifier of the artifact plugin'
  properties 'Array',      'The list of [configuration properties](#the-configuration-property-object) that represent the configuration of this artifact store.'
end
%>
