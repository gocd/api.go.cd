# Pipeline Config

The pipeline config API allows users with administrator role to manage pipeline config.

<aside class="notice">
  <strong>Important:</strong> Please note that this API requires using <code>v2</code> of the API using <code>Accept: application/vnd.go.cd.v2+json</code>
</aside>


<%= render_all_sub_topics('pipeline_config') %>

