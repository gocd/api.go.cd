## Delete Backup Config

```shell
$ curl 'https://ci.example.com/go/api/config/backup' \
      -u 'username:password' \
      -H 'Accept: <%= data.apis.versions.backup_config %>' \
      -X DELETE
```

> The above command returns JSON structured like this:

```http
HTTP/1.1 200 OK
Content-Type: <%= data.apis.versions.backup_config %>; charset=utf-8
```

```json
{
"message" : "Backup config was deleted successfully!"
}
```

<%= available_since('19.6.0') %>

<p class='http-request-heading'>HTTP Request</p>

`DELETE /go/api/config/backup`

<p class='http-request-return-description'>Returns</p>

A message confirmation if the backup config was deleted.
