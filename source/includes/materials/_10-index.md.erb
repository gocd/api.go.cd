## Get all materials

```shell
$ curl 'https://ci.example.com/go/api/materials' \
      -u 'username:password'
      -H 'Accept: <%= data.apis.versions.materials %>'
```

> The above command returns JSON structured like this:

```http
HTTP/1.1 200 OK
Content-Type: application/json; charset=utf-8
```

```json
{
  "_links": {
    "doc": {
      "href": "https://api.gocd.org/#materials"
    },
    "self": {
      "href": "https://ci.example.com/go/api/materials"
    }
  },
  "_embedded": {
    "materials": [
      {
        "description": "URL: https://github.com/gocd/gocd, Branch: master",
        "fingerprint": "03c8d2a131154436b6ef2d621c6f773837481aaea8c5c1bb9c0cb9b5bc64a2f1",
        "type": "Git"
      }
    ]
  }
}
```

Lists all available materials, these are materials that are present in the in `cruise-config.xml`.

<%= updated_to(1,'19.4.0') %>
<%= available_since('14.3.0') %>

<p class='http-request-heading'>HTTP Request</p>

`GET /go/api/materials`

<p class='http-request-return-description'>Returns</p>

An array of [material objects](#the-material-object).
