# Agents

The agents API allows users with administrator role to manage agents.

<aside class="notice">
  <strong>Important:</strong> Please note that this API requires using <code><%= short_version(data.apis.versions.agent) %></code> of the API using <code><%= data.apis.versions.agent %></code>
</aside>

<%= render_all_sub_topics('agents') %>
