# Access Tokens

Access tokens allow users to make API calls to GoCD using a bearer token instead of username and password.

<aside class="notice">
  <strong>Important:</strong> Please note that this API requires using <code>v1</code> of the API using <code><%= data.apis.versions.access_token %></code>
</aside>

<%= render_all_sub_topics('access_tokens') %>
