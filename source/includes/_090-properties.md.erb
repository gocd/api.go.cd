# Properties API


        You can get the list of properties or a property's value for a given job by using the properties API.

There is no way to delete or update a property.

<%= render_all_sub_topics('properties') %>
