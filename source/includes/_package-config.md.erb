# Packages

The package definition API <code><%= short_version(data.apis.versions.package) %></code> allows users to view, create and update the packages' configuration.

<%= render_all_sub_topics('package-config') %>
