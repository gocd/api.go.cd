# Merged Environment Config

The merged environment config API allows users with administrator role to see the configurations of environments defined in config repos.

<%= render_all_sub_topics('merged_environment_config') %>

