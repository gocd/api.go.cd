# Merged Environment Config

The merged environment config API allows users with administrator role to manage config repo environment config.

<%= render_all_sub_topics('merged_environment_config') %>

