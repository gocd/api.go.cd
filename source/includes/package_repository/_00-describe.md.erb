<%=
describe_object 'The package repository config object', since: '16.12.0' do
  id                'String',   'Each repository is associated with an id by which it can be referenced in the . If the id is not provided during creation, a uuid will be generated. Id is mandatory while providing data for update.'
  name              'String',   'The global name of the repository.'
  plugin_metadata   'Object',   'The [plugin metadata](#pluggable-scm-plugin-metadata) provides information about the plugin.'
  configuration     'Object',   'The list of [properties](#pluggable-scm-configuration) (key-value pairs) to be provided for using the plugin'
end
%>