# Server Health

The server health API allows users to monitor if the GoCD server is up and running. Usually used for health checks in Docker and Kubernetes.

<%= render_all_sub_topics('server-health') %>
