User-agent: *

# Allow root and /current
Allow: /
Allow: /current
Allow: /current/

# Disallow versioned directories
<% versions.each do |version_data| -%>
Disallow: /<%= version_data[:version] %>
<% end %>
